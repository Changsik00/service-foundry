# Grafana dashboard provisioning — 파일 기반 자동 로드.
apiVersion: 1

providers:
  - name: service-foundry
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
