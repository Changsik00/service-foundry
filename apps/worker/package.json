{
  "name": "@apps/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env --tsconfig ../../tsconfig.json src/main.ts",
    "start": "tsx --env-file-if-exists=.env --tsconfig ../../tsconfig.json src/main.ts",
    "start:prod": "tsx --tsconfig ../../tsconfig.json src/main.ts",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/backend-logger": "workspace:*",
    "@repo/backend-queue": "workspace:*",
    "tsx": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@repo/biome-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
